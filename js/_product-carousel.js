var productCarousel = tns({
  container: '.product-carousel .slider-list',
  items: 1,
  controls: false,
  mode: 'gallery',
  navContainer: '.product-carousel .pagenation',
  navPosition: 'bottom',
})

var productCarouselModal = tns({
  container: '.carousel-modal .slider-list',
  items: 1,
  loop: false,
  controlsContainer: '.carousel-modal .slider-controls',
  mode: 'gallery',
  navContainer: '.carousel-modal .pagenation',
  navPosition: 'bottom',
})
